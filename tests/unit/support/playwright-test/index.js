// Type-only stub of @playwright/test (S2.3 lint/type gate). The workspace is
// offline and the sample repos ship stub lockfiles by design, so this package
// never executes — it exists so `tsc --strict` can resolve the import.
throw new Error("@playwright/test stub: types only (S2.3 gate), not executable");