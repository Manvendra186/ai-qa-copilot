# js-monorepo (scanner sample)

pnpm-workspace sample with a React client and an Express server.
Intentionally has **no test framework** — only a manual smoke script —
to exercise the scanner's "no test framework detected" path.
Not an installable workspace member.
