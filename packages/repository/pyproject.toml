[project]
name = "qa-copilot-repository"
version = "0.1.0"
description = "Git, AST, and file indexing"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/qa_copilot_repository"]
