[project]
name = "qa-copilot-integrations"
version = "0.1.0"
description = "GitHub, Jira, CI, and other external integrations"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/qa_copilot_integrations"]
