[project]
name = "qa-copilot-knowledge"
version = "0.1.0"
description = "Retrieval, embeddings, and project memory"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/qa_copilot_knowledge"]
