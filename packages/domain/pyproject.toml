[project]
name = "qa-copilot-domain"
version = "0.1.0"
description = "Core domain models and rules"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/qa_copilot_domain"]
